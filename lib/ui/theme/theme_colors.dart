import 'package:flutter/widgets.dart';

// Add all custom colors here to easily update the theme of the app later
class ThemeColors {
  static const Color backgroundBlue = Color.fromARGB(255, 226, 233, 237);
}
